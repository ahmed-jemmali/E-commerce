import { SupplierCategoryFilterPipe } from './supplier-category-filter.pipe';

describe('SupplierCategoryFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SupplierCategoryFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
