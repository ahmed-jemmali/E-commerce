import { PurchaseInvoiceFilterPipe } from './purchase-invoice-filter.pipe';

describe('PurchaseInvoiceFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PurchaseInvoiceFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
