import { PurchaseProductFilterPipe } from './purchase-product-filter.pipe';

describe('PurchaseProductFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PurchaseProductFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
