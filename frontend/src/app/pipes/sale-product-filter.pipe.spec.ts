import { SaleProductFilterPipe } from './sale-product-filter.pipe';

describe('SaleProductFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SaleProductFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
