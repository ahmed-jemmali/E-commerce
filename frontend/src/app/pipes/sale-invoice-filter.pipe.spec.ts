import { SaleInvoiceFilterPipe } from './sale-invoice-filter.pipe';

describe('SaleInvoiceFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SaleInvoiceFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
